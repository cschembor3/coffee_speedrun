<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8"/>
    <meta http-equiv="X-UA-Compatible" content="IE=edge"/>
    <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
    <% if assigns[:run_type] == "coffee" do %>
      <title>CoffeeSpeedrun</title>
    <% else %>
      <title>CookieSpeedrun</title>
    <% end %>
    <link rel="stylesheet" href="<%= Routes.static_path(@conn, "/css/app.css") %>"/>
    <link rel="icon" type="image/png" href= "<%= Routes.static_path(@conn, "/images/icons8-hot-coffee-64.png") %>" />
  </head>
  <body>
    <header>
      <section class="container">
        <nav class="nav">
          <ul>
            <li><a href="/about.html">About</a></li>
            <li><a href="/rules.html">Rules</a></li>
            <li><a href="<%= assigns[:run_type] %>/runners">Leaderboard</a></li>
          </ul>
        </nav>
        <div class="home_button">
          <a href="
            <%=
              if assigns[:run_type] == "coffee" do "/coffee"
              else "/cookie" end
            %>">
            <h1>
              <%= 
                if assigns[:run_type] == "coffee" do "Coffee"
                else "Cookie" end
              %> Speedrun
            </h1>
          </a>
          <a href="/homepage.html">Other speedruns</a>
        </div>
      </section>
    </header>
    <main role="main" class="container">
      <p class="alert alert-info" role="alert"><%= get_flash(@conn, :info) %></p>
      <p class="alert alert-danger" role="alert"><%= get_flash(@conn, :error) %></p>
      <%= render @view_module, @view_template, assigns %>
    </main>
    <script type="text/javascript" src="<%= Routes.static_path(@conn, "/js/app.js") %>"></script>
</html>
